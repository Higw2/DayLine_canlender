# macOS 可拖拽分区验收

- 验收日期：2026-09-10
- 验收角色：Sol（medium）独立验收
- 产物：`dist/DayLine-macOS.zip`
- 隔离数据目录：`/private/tmp/dayline-split-sol-acceptance`
- 结论：有条件通过。分区核心闭环和自动化测试通过，未发现实现缺陷；时间线拖拽新建与分隔线同时交互的组合场景因 CUA 坐标映射故障未完成实机验证。

## 自动化与静态检查

- `swift test`：12 项通过，0 失败。覆盖默认值、旧配置缺少 `sidebar_ratio`、越界值夹取、JSON 往返、横竖方向阈值、像素最小值和 resize 后恢复保存比例。
- `git diff --check`：通过。
- 静态确认默认比例为 35%，保存范围为 18%–55%；宽度小于 960pt 或宽度小于高度时上下排列，否则左右排列。
- 静态确认 `splitViewDidResizeSubviews` 仅按内存中的已保存比例重排；只有 `TrackingSplitView.mouseDown` 完成原生分隔线跟踪后才调用保存回调。
- 静态确认旧设置解码缺少 `sidebar_ratio` 时回退到 0.35，异常比例在解码时夹取到合法范围。
- 静态确认侧栏使用 `ScrollView`，detail header 使用 `ViewThatFits` 提供单行/两行布局，时间线最小宽度为 260pt。
- 静态确认原背景视图仍包裹主界面，顶部“＋ 新建事件”入口和时间线 `newRange` 回调仍连接到原事件编辑流程。

## 实机 GUI 结果

| 场景 | 结果 | 证据 |
| --- | --- | --- |
| 首次启动默认比例 | 通过 | 1120pt 初始窗口中 splitter 为 391.5pt，约为可用宽度的 35%。 |
| 向右拖到上限 | 通过 | splitter 停在 615.5pt；松手后 `settings.json` 写入 `sidebar_ratio: 0.55`。窄 detail header 自动变为两行，按钮仍可见。 |
| 向左拖到下限 | 通过 | 当前 1120pt 窗口受侧栏 220pt 最小像素约束，splitter 停在 220pt；松手保存 0.1966041108132261，位于 18%–55% 范围内。 |
| 重启恢复 | 通过 | 结束进程并以同一隔离目录重启后，splitter 恢复为 220pt。 |
| resize 不覆盖比例 | 通过 | 将窗口宽度缩到 960pt 以下后自动变为上下分区，侧栏出现滚动条；恢复宽屏后回到左右分区。两次读取设置文件，比例均保持 0.1966041108132261。 |
| 方向切换 | 通过 | 窄屏时 splitter 变为水平分隔，恢复宽屏时变回垂直分隔，内容和按钮保持可见。 |
| 130% 字体 | 通过 | 在隔离设置中设 `font_scale: 1.3` 后重启，侧栏、顶部按钮、日期标题和时间刻度均正常显示，无明显裁切或重叠。 |
| 时间线拖拽新建不与分隔线冲突 | 未完成 | CUA 对时间线坐标错误映射到窗口外并返回 `windowNotFoundAtPosition`；随后 AX 点击 scroll area 不会产生拖拽范围。应用进程保持运行，未观察到应用崩溃，因此这是验收工具限制，不能据此判定应用失败。 |

## 边界说明

- 在当前 1120pt 初始窗口中，18% 对应宽度小于侧栏 220pt 最小值，因此实机左端由 220pt 像素下限决定；18% 数值下限由单元测试与静态代码检查确认。
- 偏好设置 sheet 会使本次 CUA native pipe 关闭，因此 130% 字体通过隔离配置文件写入后重启验证；字体缩放后的最终主界面已实机检查。
- 未改动或读取用户的真实 DayLine 数据，也未连接用户当前运行实例。
