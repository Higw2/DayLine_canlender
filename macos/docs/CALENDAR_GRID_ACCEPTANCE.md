# macOS 响应式月历与时间线验收

- 验收日期：2026-09-10
- 验收角色：Sol（medium）独立验收
- 验收产物：`macos/build/DayLine.app`、`dist/DayLine-macOS.zip`
- 隔离数据目录：`/private/tmp/dayline-calendar-grid-sol-acceptance-retry`
- 结论：通过本次“月历居中短小、未随窗口适配”修复。未发现需要回交实现代理的业务缺陷。窄窗与 55% / 130% 的三项组合因 CUA 窗口坐标及连接故障未在本轮重新完成 GUI 截图，不能视为本轮组合实测；前一轮同一分区实现已分别实测窄窗方向切换、55% 侧栏和 130% 字体。

## 自动化与静态检查

- `swift test`：15 项通过，0 失败。新增覆盖月历恒定 42 格、周一为每周首日、闰月和自定义首日、随单元格宽度与字体比例变化的行高。
- `git diff --check`：通过。
- ZIP 内可执行文件与 `macos/build/DayLine.app` 的 SHA-256 相同：`1a00661f7162c4d464c857ddf75fc720e5a8c7390e7d88c19810c0f38450de57`。
- 月历使用父容器可用宽度除以 7 计算单元格宽度；7 列和 6 行始终输出 42 个日期。日期行高随列宽增加，并限制在 `28 * fontScale` 至 `48 * fontScale`。
- 相邻月份日期保留在网格内并降低到 42% 亮度；点击日期会同步选择日、显示月份并刷新右侧时间线。
- 时间线视图宽度绑定 detail 的 `GeometryReader` 宽度；`TimelineNSView` 在 frame 或 bounds 改变时请求重绘，横线、当前时间线、选择区域和事件卡片均按当前 bounds 计算到右边缘。
- 宽度小于 960pt 或窗口呈纵向时，既有 `MainSplitLayout.shouldStack` 仍切换为上下分区；本次月历修改没有改变该逻辑。

## 本轮 GUI 实测

| 场景 | 结果 | 证据 |
| --- | --- | --- |
| 默认 1120pt、约 36% 侧栏 | 通过 | AX splitter 为 404pt。可见截图中月历从侧栏左右内边距之间完整铺开，7 列日期均匀占满，不再以短表格居中；时间线横线从 64pt 刻度槽延伸到 detail 右边缘。 |
| 固定 42 格与相邻月弱化 | 通过 | AX 树列出 2026-08-31 至 2026-10-11 共 42 个按钮；截图可见前后月日期亮度低于当月日期。 |
| 上月、下月、相邻月日期与返回今天 | 通过 | 下月按钮切到 2026 年 10 月；点击相邻的 9 月 30 日后月历回到 9 月，右侧标题同步为“2026年09月30日 星期三”；“返回今天”恢复 9 月 10 日和右侧标题。 |
| 55% 侧栏 + 130% 字体 | 通过 | 通过隔离设置重启后 AX splitter 为 615.5pt。截图显示月历扩展至侧栏有效宽度，字号、列距和行距同步增大，无裁切或重叠；detail 顶部自动分为两行并保留全部按钮。 |
| 超宽窗口 | 通过 | 放大窗口后 AX splitter 为 808pt；月历继续横向铺满宽侧栏，行高受到上限约束，没有无限拉长。时间线横线和事件卡片仍到达 detail 右侧。 |
| 时间线事件卡片铺满 | 通过 | 在隔离数据库建立 09:00–10:00 的“验收宽度”事件；截图显示单列卡片从刻度槽后延伸到 detail 右内边距，并随超宽 resize 重新铺满。 |
| 时间线空白拖建 15 分钟 | 通过 | 在空白时间线拖动后打开事件编辑器，开始时间为 05:30、结束时间为 05:45；随后取消草稿，没有写入事件。 |
| 840×600 与小于 960pt 上下布局 | 本轮未重新 GUI 实测 | CUA 对窗口/分隔线坐标出现 `noWindowsAvailable`、`windowNotFoundAtPosition`，且不支持 `fn` 窗口缩放键。本轮没有据静态代码宣称 GUI 通过；`SPLIT_ACCEPTANCE.md` 已记录同一分区实现此前在小于 960pt 时切为水平分隔、恢复宽屏后回到垂直分隔，当前方向与最小值单测通过。 |
| 55% + 窄窗 + 130% 三项组合 | 本轮未组合复验 | 本轮只完成 55% + 130% 在默认宽度及超宽窗口的截图；受上述 CUA 故障影响，没有取得三项同时存在时的窄窗截图。 |

## 验收边界

- 本轮仅操作隔离数据目录；“验收宽度”测试事件只存在于该临时数据库。
- CUA 最初曾绑定同 bundle id 的旧窗口；确认并关闭旧实例后，当前唯一进程绑定隔离目录，随后所有本轮核心截图均来自隔离实例。
- `SPLIT_ACCEPTANCE.md` 是先前分区功能的独立验收记录，只用于说明窄窗行为已有实机覆盖；本文件没有把该历史结果冒充为本轮截图。
